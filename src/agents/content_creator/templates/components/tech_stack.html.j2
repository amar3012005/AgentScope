<div class="flex flex-wrap gap-2">
  {% for item in items %}
  <span class="px-3 py-1.5 text-xs font-mono text-white/80 bg-white/5 border border-white/10 rounded-full">{{ item }}</span>
  {% endfor %}
</div>
